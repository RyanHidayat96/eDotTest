appId: ${EWORK_APP_ID}
---
# Probe-only fallback. Runtime test uses ADB hierarchy comparison to stop when list cannot scroll further.
- repeat:
    times: 80
    commands:
      - swipe:
          start: "50%, 88%"
          end: "50%, 30%"
          duration: 10
          waitToSettleTimeoutMs: 0
